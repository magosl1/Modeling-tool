-r requirements.txt

pytest==8.2.0
pytest-cov==5.0.0
ruff==0.5.0
# httpx is pinned in requirements.txt (<0.28 to satisfy litellm 1.55.3).
# Do not double-pin here — it would create a resolver conflict on CI.
